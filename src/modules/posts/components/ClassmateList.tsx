import { Box, Typography } from '@mui/material';

const ClassmateList = () => (
  <Box sx={{ minWidth: '16rem', px: 2 }}>
    <Typography>Bạn cùng lớp</Typography>
  </Box>
);

export default ClassmateList;
